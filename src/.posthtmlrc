{
  plugins: {
    "posthtml-include": {
      root: "src",
    },
    "posthtml-modules": {
      root: "src", // for absolute paths
      from: "./src/index.html", // for relative paths
      plugins: [],
    },
  },
}
